{
  "name": "autodrop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Autonomous agent whose terminal goal is maximizing $ in Stripe balance.",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "jose": "^5.10.0",
    "tsx": "^4.19.0",
    "turbo": "^2.3.0",
    "typescript": "^6.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "@clerk/shared"
    ]
  },
  "dependencies": {
    "convex": "^1.38.0"
  }
}
